services:
  - type: web
    name: indoads-prompt-mcp
    runtime: docker
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 3000
      - key: INDOADS_API_KEY
        generateValue: true
      - key: PUBLIC_BASE_URL
        sync: false
